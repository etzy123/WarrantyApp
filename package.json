{
  "name": "fairway-route",
  "version": "1.0.0",
  "description": "Bisque Golf warranty claim portal — routes claims to the right brand contact via Shopify order lookup, and emails updates via SendGrid.",
  "main": "src/index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "migrate": "node src/migrate.js"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.2.0",
    "pdfkit": "^0.20.1",
    "pg": "^8.12.0"
  }
}
